interface:
  display_name: "Swift Mlx Forge"
  short_description: "Repo-specific patterns and workflows for swift-mlx-forge"
  default_prompt: "Use the swift-mlx-forge repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true